[package]
name = "abi-root-fixture-host"
version = "0.1.0"
edition = "2024"
rust-version = "1.97.1"
publish = false

[dependencies]
abi_stable = { version = "=0.11.3", default-features = false }
abi-root-fixture-contract = { path = "../abi-contract" }

[workspace]
