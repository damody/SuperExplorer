# Dual code-lines visual review

Reviewer: primary integrator
Date: 2026-08-05 Asia/Taipei
Final screenshot: `headful-final/code-lines.png`
SHA-256: `218DEAF56BB7ABB6148C6D28AB425D997B0A5706E55F1F2E7A64C700E009BB0C`
Detail screenshot: `headful-final/code-lines-detail.png`
SHA-256: `858F22B6532F3D9E9ECECD5BADA3E906BDFC610DF7945EA0483E2973B0220727`

Subchecks:

- `3.2.3/headers`: PASS — `Code lines` and `Main code lines` are simultaneously visible.
- `3.2.3/lua-values`: PASS — Lua `Code lines` retains numeric labels, including `1250`.
- `3.2.3/rust-values`: PASS — Rust `Main code lines` shows language and count, including exact `Rust: 1,250`.
- `3.2.3/alignment`: PASS — values appear beneath the matching headers after sorting runtime/visual pairs by stable column identity.
- `3.2.3/clipping`: PASS — both headers and `Rust: 1,250` are fully readable.
- `3.2.3/stale-or-missing`: PASS — all four supported fixture files populate both columns and no loading state remains.
- `3.2.3/bars`: PASS — UI Automation exposes no `code-lines-bar-track-*` elements.
- `3.2.3/sort`: PASS — Rust numeric ascending order is `1, 1, 1, 1250`.

Iteration lineage:

1. Initial dual run failed on duplicate accessibility IDs; cell IDs were namespaced by stable column identity.
2. Second run exposed header/value order mismatch and a pixel-heuristic false positive; row render pairs were sorted to registry identity order and bar verification moved to UI Automation.
3. Third run passed coexistence but used small values; a deterministic 1,250-line Rust fixture was added.
4. Final run passed every visual subcheck and exited cleanly.

Task `3.2.4` disposition: superseded failure evidence is retained in `headful/`, `headful-pass2/`, and `headful-pass3/`; final passing evidence is `headful-final/`.
