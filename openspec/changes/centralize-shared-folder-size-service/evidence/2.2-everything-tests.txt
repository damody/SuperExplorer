2026-08-14 G4 Everything boundary evidence

- `cargo test -p explorer-shell-win --lib everything --locked -- --test-threads=1`
- Result: 8 passed, 0 failed.
- Full-path, live kind/size, canonical `\\?\` root containment, stale path,
  escaped prefix, mutation mismatch, cancellation and eligible-node quota checks pass.
- Everything 1.4 supplies no transaction/checkpoint proving complete current subtree
  coverage, so the folder snapshot adapter remains intentionally ineligible and falls
  through to recursive reference instead of publishing an unproven exact zero.
