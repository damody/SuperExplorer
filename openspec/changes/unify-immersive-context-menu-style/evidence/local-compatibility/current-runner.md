# Local Shell compatibility — current runner

## Built-in and menu-form matrix

`real_explorer_profile_baseline_captures_labels_for_every_target_and_shift_profile` passed for
background, one file, one folder, and multi-select under ordinary and Shift-extended profiles.
The trace retained labels, command IDs, depths, canonical verbs, and host/provider routing.
Controlled HMENU tests additionally passed duplicate IDs, checked, disabled, bitmap,
separator, nested submenu, dynamic `WM_INITMENUPOPUP`, and owner-draw fallback forms.

## Installed handlers observed

The real inventory included WinRAR, 7-Zip, TortoiseGit, VS Code, Microsoft Defender,
Visual Studio, Git GUI/Bash, and other installed handlers. The headful Local screenshot also
shows transparent extension icons in the application-owned popup.

## 7-Zip reversible invocation

`installed_7zip_extension_queries_submenu_and_invokes_owned_archive_command` passed when run
explicitly with ignored tests enabled. It traversed nested 7-Zip and CRC/SHA submenus, selected
the collision-owned `加入 "third-party.7z"` command, created a non-empty archive inside the
temporary fixture, and released the fixture afterward.

## Defender cancellation

Microsoft Defender population was present in file, folder, and multi-select inventories. The
real 1,000-cycle application-owned cancellation soak queried menus containing that handler and
never invoked a scan.

WinRAR, TortoiseGit, and VS Code have inventory/display evidence, but their separate reversible
invocation procedures remain open and are not marked complete.
