# G-PERF status

One diagnostic focused run reported `no_disk_us=90812`, `windows_cache_us=3510`, and `project_disk_us=1023`; it is useful for smoke detection only. It is not a frozen multi-run Release A/B study and contains no adapter/driver, frame-time distribution, repeated-navigation working-set, or sustained-growth dataset.

G-PERF is therefore unresolved. Raw failures/missing fields are preserved by leaving tasks 5.3.1 through 5.3.7 open and keeping both rollout defaults disabled.
