## 1. Reproduction and contract

- [x] 1.1 Add focused unit coverage proving a bare drive designator becomes an absolute root while ordinary paths and namespaces remain stable.
- [x] 1.2 Add a restored-session headful fixture with two tabs, high-DPI placement, an active bare drive root, and no topmost window assistance.

## 2. Implementation

- [x] 2.1 Normalize a successfully resolved bare Windows drive designator in the Shell location publication boundary.
- [x] 2.2 Verify the repaired descriptor reaches active history and session persistence through the existing resolved-location flow.

## 3. UTIT and regression verification

- [x] 3.1 Physically right-click a non-first restored drive-root item and assert exact selection plus one process-bound native popup.
- [x] 3.2 Map the restored drive-root requirement to strict UTIT manifest coverage.
- [x] 3.3 Pass targeted Rust tests, existing context-menu headful cases, and strict OpenSpec validation.
- [x] 3.4 Build and validate debug and release binaries, then commit the isolated fix without touching unrelated workspace content.
- [x] 3.5 Ensure the manifest-driven headful case rebuilds the current application so a standalone UTIT run cannot exercise a stale debug executable.
