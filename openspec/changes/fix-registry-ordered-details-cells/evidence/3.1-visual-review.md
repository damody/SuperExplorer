# Headful visual review

Reviewer: primary integrator
Date: 2026-08-05 Asia/Taipei
Final screenshot: `evidence/headful-wide/code-lines.png`
Screenshot SHA-256: `800873CA494BB658295A9F0FDAD0A001C8F4731AD6040FEC384B03615C1E3D91`
Report SHA-256: `8FFA2C89D6F37861708F9DA85C44F8BAA8774455282C5C7B066B449E1D4E6CEA`

## Review

- PASS: Header order is `Code lines`, `Folder size`, `Lock owners`, `Main code lines`, matching stable registry ID order.
- PASS: `Code lines` contains Lua numeric counts (`1250`, `1`, `1`, `1`) and no folder-size bars.
- PASS: `Folder size` contains proportional bars plus byte values (`20.9 KB`, `17 B`, `16 B`, `12 B`).
- PASS: `Lock owners` retains its own empty cells for this unlocked fixture and does not borrow adjacent values.
- PASS: `Main code lines` contains language-qualified values including exact `Rust: 1,250`.
- PASS: No clipping, stale empty slot, duplicate accessibility identity, or header/data swap is visible.
- PASS: UI Automation bounding-rectangle assertions independently verified populated `Code lines`, `Folder size`, and `Main code lines` cells lie within their matching headers.
- PASS: App shut down cleanly and reported no unexpected plugin/tool descendants.

## Iteration lineage

1. The first directory run used the old ten-row threshold with the deterministic four-file dual fixture and failed after capturing a correct partial screenshot; this was a harness threshold defect, not an app defect.
2. A non-directory run reached the optional detail-menu branch and exposed an unrelated context-menu selection limitation after capturing correct values.
3. The final directory run used a four-row dual-fixture threshold and a wide off-screen capture so all four extension headers and values are visible simultaneously; it passed every blocking semantic, visual, shutdown, and process check.
