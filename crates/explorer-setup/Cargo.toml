[package]
name = "explorer-setup"
version = "0.1.0"
description = "First-party SuperExplorer test installer (non-NSIS) for other Windows PCs."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true

[[bin]]
name = "superexplorer-setup"
path = "src/main.rs"

[dependencies]
anyhow.workspace = true
tempfile.workspace = true
zip.workspace = true

[target.'cfg(windows)'.dependencies]
windows.workspace = true

[dev-dependencies]
tempfile.workspace = true

[build-dependencies]
embed-resource.workspace = true

[lints]
workspace = true
